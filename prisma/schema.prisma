// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
  
}

enum TipoProduto {
  frasco
  equipo
  kit
  outro
}

model Anuncio {
  id                String   @id @default(uuid())
  link              String   @unique
  tituloAnuncio     String
  precoEmCentavos   Int
  quantidadeEmItens Int?
  disponivel        Boolean  @default(true)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  tipoProduto       TipoProduto
  // Autoreferência para variantes
  anuncioPrincipalId String?
  anuncioPrincipal   Anuncio?  @relation("AnuncioVariantes", fields: [anuncioPrincipalId], references: [id], onDelete: Cascade)
  variantes          Anuncio[] @relation("AnuncioVariantes")
  slug              String   @unique
  provedorDaBusca   String  
}
